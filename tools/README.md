# NetShark 工具目录

此目录用于存放部署所需的工具和安装包。

## 需要放置的文件

### 1. Npcap 安装包（必需）
下载地址: https://npcap.com/#download

请将下载的安装包重命名或直接放置为：
- `npcap-1.85.exe` 或
- `npcap-1.80.exe`

安装时请勾选 "WinPcap API-compatible Mode" 选项。

## 说明

- install.bat 会自动检测并提示安装 Npcap
- 如果用户已安装 Npcap，则会跳过
